package at.geyser.cashier;

/**
 * Interface for every controller.
 */
public interface Controller {

    /**
     * Init method which is called after the controller is created.
     */
    public void init();
}